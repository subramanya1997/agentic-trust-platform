# App Configuration
DEBUG=true
APP_NAME="Agentic Trust API"
API_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Database (PostgreSQL)
DATABASE_URL=postgresql+asyncpg://agentic:agentic_dev_password@localhost:5432/agentic_trust

# Cache (Redis)
REDIS_URL=redis://localhost:6379

# WorkOS Authentication (get from https://dashboard.workos.com)
WORKOS_API_KEY=sk_test_xxx
WORKOS_CLIENT_ID=client_xxx
WORKOS_COOKIE_PASSWORD=a-32-character-or-longer-secret-for-sessions
WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback

# Encryption Key (generate with: openssl rand -base64 32)
ENCRYPTION_KEY=

# LLM Providers (optional for local dev)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# GCP (optional for local dev)
GCP_PROJECT_ID=
